# Single-gateway mode: the Node server serves this frontend and proxies the nav
# service, so all API calls are same-origin (empty base = relative URLs). This
# lets the whole app run behind one URL / one tunnel.
VITE_SERVER_DOMAIN=
VITE_MANAGER_PIN=1234
